name: im_web
description: im_web_app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  flutter_svg: ^1.0.0
  fl_chart: ^0.40.5
  provider: ^6.0.1
  page_transition: ^2.0.4
  dotted_border: ^2.0.0+2
  dropdown_button2: ^1.0.7
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/